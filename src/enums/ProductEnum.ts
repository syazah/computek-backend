export enum Applicability {
    PER_JOB = "per-job",
    PER_BATCH = "per-batch",
    PER_SHEET = "per-sheet"
}

export enum CostItemEnum {
    FOLDING = "FOLDING",
    LAMINATION = "LAMINATION",
    UV = "UV",
    FOIL = "FOIL",
    DIE = "DIE",
    TEXTURE = "TEXTURE"
}