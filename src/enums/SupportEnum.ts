export enum SupportStatus {
    OPEN = "open",
    IN_PROGRESS = "in_progress",
    RESOLVED = "resolved",
    CLOSED = "closed"
}

export enum SupportPriority {
    LOW = "low",
    MEDIUM = "medium",
    HIGH = "high"
}